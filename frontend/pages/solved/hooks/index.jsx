import classes from './styles.module.css'

export default function Hooks() {
  return <div className={classes.main}>
    <a href={'/solved/hooks/interval'}>interval</a>
    <a href={'/solved/hooks/debounce'}>debounce</a>
  </div>
}

